# Backend service granted to every organization unit in the deployment
resource_type: application
id: decl-m2m-all-ous
name: M2M Service (All OUs)
description: Backend service granted to every organization unit in the deployment
ouId: decl-m2m-root
type: fullstack
template: web
url: https://example.com
grants:
  - allOus: true
inboundAuthConfig:
  - type: "oauth2"
    config:
      clientId: "decl-m2m-all-ous-client"
      clientSecret: "decl-m2m-all-ous-secret"
      grantTypes:
        - "client_credentials"
      tokenEndpointAuthMethod: "client_secret_basic"
      pkceRequired: false
      publicClient: false
      token:
        accessToken:
          clientConfig:
            # Opt in to the organization unit claims so the token states which organization it was
            # issued for. On an /ou/{ouId} request these resolve to the accessing OU, not the owner.
            attributes:
              - ouId
              - ouName
              - ouHandle
